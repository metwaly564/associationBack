// Local PostCSS config for admin-cms to avoid ESM/CJS conflict
// If you already use PostCSS/Tailwind in the root project,
// you can adapt these plugins to match your needs.

module.exports = {
  plugins: {
    autoprefixer: {},
  },
};

